# Track 027 internal review

Construction-PQ kill is justified: 14k dist is 14.55% of create; encode+LUT cannot beat the remaining graph/heaps wall; `k_dist` stays full-vector. `train_pq` is 20 s post-hoc, not a create substitute.

Landed: L2 guard, centroids 1..=256, exact v_dist re-rank. Convert won’t-do. D-012-01 not closed.

No findings above low.
